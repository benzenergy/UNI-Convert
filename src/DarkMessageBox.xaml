<Window x:Class="uniconvert.DarkMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resource/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Внешний контейнер с Margin для тени -->
    <Grid Margin="20">
        <!-- оставляем место для тени -->

        <Border CornerRadius="15"
                Background="#2B2B2B"
                BorderBrush="#3C3C3C"
                BorderThickness="1"
                Padding="25">

            <!-- Тень -->
            <Border.Effect>
                <DropShadowEffect 
                    Color="Black"
                    BlurRadius="20"   
                    ShadowDepth="0"
                    Opacity="0.5" />
            </Border.Effect>

            <StackPanel>
                <TextBlock x:Name="MessageText"
                           Foreground="White"
                           FontSize="14"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>
                
                <Button Style="{StaticResource SettingsButtonStyle}"
                        Content="ОК"
                        Width="80"
                        Height="30"
                        HorizontalAlignment="Center"
                        Click="Ok_Click"
                        Background="#252525"
                        BorderBrush="#3C3C3C"/>
            </StackPanel>

        </Border>
    </Grid>
</Window>